<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ktdsuniversity.edu.files.dao.FilesDao">

	<insert id="insertAttachFile" parameterType="com.ktdsuniversity.edu.files.vo.request.UploadVO">
		INSERT INTO TMDB.FILES
		       (FILE_NUM
		     , FILE_GROUP_ID
		     , OBFUSCATE_NAME
		     , DISPLAY_NAME
		     , EXTEND_NAME
		     , FILE_LENGTH
		     , FILE_PATH)
		VALUES ((SELECT NVL(MAX(FILE_NUM),0)+1
				  FROM FILES
				 WHERE FILE_GROUP_ID = #{fileGroupId})
		     , #{fileGroupId}
		     , #{obfuscateName}
		     , #{displayName}
		     , #{extendName}
		     , #{fileLength}
		     , #{filePath})
	</insert>
	
<select id="selectFilesByGroupIdAndFileNum" 
        parameterType="com.ktdsuniversity.edu.files.vo.request.SearchFileVO" 
        resultType="com.ktdsuniversity.edu.files.vo.response.DownLoadVO">
    SELECT FILE_NUM
         , FILE_GROUP_ID
         , OBFUSCATE_NAME
         , DISPLAY_NAME
         , EXTEND_NAME
         , FILE_LENGTH
         , FILE_PATH
      FROM TMDB.FILES
     WHERE FILE_GROUP_ID = #{fileGroupId}
       AND FILE_NUM = #{fileNum}
</select>
  

<select id="selectFilesByGroupId"  parameterType="string" resultType="string">
    SELECT FILE_PATH
  FROM TMDB.FILES
 WHERE FILE_GROUP_ID = #{id}
</select>
    

</mapper>




